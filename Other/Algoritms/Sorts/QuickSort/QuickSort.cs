using System.ComponentModel.DataAnnotations;
using System.Linq;
using QuickSort.Interface;

namespace QuickSort
{
    public class QuickSort // : ISort
    {
        //int[] Sort(int[] array)
        //{
        //    if (array.Length < 2)
        //    {
        //        return array;
        //    }
        //    else
        //    {
        //        int pivot = array.First();

        //        var less = array.Where(a => a < pivot).ToArray();
        //        var greater = array.Where(a => a > pivot).ToArray();

        //    }
        //}
    }
}